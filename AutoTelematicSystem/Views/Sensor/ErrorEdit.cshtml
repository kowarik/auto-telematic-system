@model GetErrorDto
@{
    ViewData["Title"] = "Edit Error";
}

<div class="container my-5">
    <h2 class="text-primary"><i class="bi bi-pencil-square"></i> Edit Error</h2>
    <form asp-action="ErrorEdit" method="post" class="shadow p-4 bg-light rounded">
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="SensorTypeId" />

        <div class="mb-3">
            <label asp-for="Code" class="form-label">Code</label>
            <input asp-for="Code" class="form-control" />
            <span asp-validation-for="Code" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Description" class="form-label">Description</label>
            <textarea asp-for="Description" class="form-control" rows="4"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <div class="d-flex justify-content-between">
            <a class="btn btn-secondary" asp-action="ErrorList" asp-route-sensorTypeId="@Model.SensorTypeId">
                <i class="bi bi-arrow-left"></i> Back to List
            </a>
            <button type="submit" class="btn btn-primary">
                <i class="bi bi-save"></i> Save Changes
            </button>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
